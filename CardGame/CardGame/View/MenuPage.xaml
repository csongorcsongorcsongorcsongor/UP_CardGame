<Page x:Class="CardGame.View.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CardGame.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MenuPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0">RNGesus</Label>
        <Button Grid.Row="1" Command="{Binding SetDifficultyCommand}" CommandParameter="Easy" IsEnabled="{Binding Easy}" >Easy</Button>
        <Button Grid.Row="2" Command="{Binding SetDifficultyCommand}" CommandParameter="Medium" IsEnabled="{Binding Medium}" >Medium</Button>
        <Button Grid.Row="3" Command="{Binding SetDifficultyCommand}" CommandParameter="Hard" IsEnabled="{Binding Hard}" >Hard</Button>
        <Button Grid.Row="4 " Command="{Binding PLayCommand}">Play</Button>
    </Grid>
</Page>
